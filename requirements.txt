fastapi
uvicorn[standard]
httpx
mwparserfromhell
python-dotenv    # arrives via uvicorn[standard] too, but the server imports it directly
pytest
psycopg[binary]  # only imported when DATABASE_URL is set
ruff            # lint step in CI
